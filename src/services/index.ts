import {
  generateQuiz,
  retrieveQuiz,
  retrieveResults,
  submitResponse,
} from "@@services/quiz.service";

// Exporting service functions for use in controllers or other parts of the application
export { generateQuiz, retrieveQuiz, retrieveResults, submitResponse };
