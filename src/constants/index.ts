// Importing HTTP status codes from the statuscode.constants file
import { HTTP_STATUS_CODE } from '@@constants/statuscode.constants';

// Importing message constants from the message.constants file
import { MESSAGES } from '@@constants/message.constants';

// Exporting the imported constants so they can be used in other parts of the application
export {
  HTTP_STATUS_CODE,
  MESSAGES
};
